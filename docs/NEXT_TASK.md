# Next task

Validate the read-only account, holdings, pending-order, and buying-power response shapes in a Toss paper or otherwise authorized environment. Confirm that `pending_orders()` uses the correct production query parameter, then add a captured non-secret fixture if needed.

Do not enable live orders as part of this validation.
